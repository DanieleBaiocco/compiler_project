package it.unicam.cs.compilatore_re.thompsonNFA;

/**
 * Enumerazione che rappresenta gli stati in cui uno stato di un automa a stati finiti
 * non deterministico può essere: iniziale, finale e intermedio, se non è né iniziale né finale
 */
public enum StateEnum {
    INITIAL, FINAL, MIDDLE
}
